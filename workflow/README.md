# Workflow

Add a sanitised n8n workflow export here.

Do not commit the raw production workflow export. Before publishing, remove:

- credential IDs
- credential names
- webhook IDs
- Gmail label IDs
- personal email addresses
- real client names
- real phone numbers
- Google Drive folder IDs
- execution data
- private prompt fragments that reveal client processes

Recommended filename:

```text
copywriting-pipeline.sanitised.json
```
